Приложение для управления базой колцентра в веб. Сделано для теста где-то за день

Новые базы загружаются из csv файлов.
По клику на порядковый номер можно открывается изменение этой конкретной строки таблицы. Некоторые поля просчитываются сами, их нельзя менять вручную.

Примеры баз в csv находятся в директории callcetner/базы, можно загружать их на главной странице.
Должен быть заголовок и поля в том же порядке, дата в формате либо ГГГГ-ММ-ДД либо ДД.ММ.ГГГГ, id проставляется автоматически.


Можно отображать 10-100 строк на станицу, базы разделяются на страницы. Если надо добавить новые поля, внизу есть кнопка Добавить.
Нажав на нее можно загрузить csv файл и он загрузится в эту конкретную базу/файл. Если в файле есть абсолютно идентичные с базой поля, эти поля не загрузятся, 
грузится только разница.

На странице базы есть мгновенный поиск по всей таблице. По щелчку на заголовок можно сортировать таблицу по конкретному столбцу.
Фильтр есть один для примера - по цене, такие же можно сделать по другим полям.



Для запуска приложения:
установить зависимости
pip install -r requirements.txt
создать базу данных

python manage.py makemigrations
python manage.py migrate
python manage.py runserver
